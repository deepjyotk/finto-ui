# Same OAuth 2.0 Web client ID as backend GOOGLE_CLIENT_ID
NEXT_PUBLIC_GOOGLE_CLIENT_ID=

# When true, hide username/password login and sign-up forms in the UI (Google sign-in stays visible)
NEXT_PUBLIC_HIDE_BASIC_AUTH=false
